const axios = require('axios').default;

const getUserIntagram = async function(req, res) {
    const info_user_media = await axios
        .get()
        .then()
        .catch()
}

module.exports = {
    getUserIntagram
}